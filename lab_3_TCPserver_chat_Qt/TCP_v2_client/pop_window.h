#ifndef POP_WINDOW_H
#define POP_WINDOW_H

#include <QWidget>

class pop_window : public QWidget
{
    Q_OBJECT
public:
    explicit pop_window(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // POP_WINDOW_H